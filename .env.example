# Copy this file to .env and adjust the values for your local setup.
# Never commit the real .env file.

APP_NAME=ragbridge

# "development" or "production". In production the app refuses to start if
# DATABASE_URL still uses the ragbridge:ragbridge credentials below - they are
# published in this file, so they are public. Generate a real one with:
#   openssl rand -hex 24
# (hex, not base64: the password sits inside a URL, and base64's / + = are
# URL delimiters that some parsers misread.)
ENVIRONMENT=development
DATABASE_URL=postgresql+psycopg://ragbridge:ragbridge@localhost:5432/ragbridge
MAX_UPLOAD_SIZE=10000000

# How PDF pages are read. "auto" reads each page row by row, so a side column
# (a CV's dates and cities) stays beside its text, and keeps pypdf's plain order
# for pages of two columns of prose. "plain" is pypdf's default everywhere;
# "layout" always reads row by row. Only documents uploaded after a change are
# affected: delete and re-upload a document to read it again.
PDF_EXTRACTION=auto

# Serve the generated API docs at /docs, /openapi.json and /redoc. Every
# endpoint needs an API key either way, so this controls whether the API's
# shape is public - not its data. Turn it off on a public deployment.
ENABLE_DOCS=true

# Serve the playground at /playground: a page for trying the service on your
# own files and seeing which chunks retrieval found. It holds no key and
# reads no data until you paste an API key into it, but it is a development
# tool - turn it off on a public deployment (the production Compose file does).
ENABLE_PLAYGROUND=true

# Embeddings and chat. Defaults use a local Ollama, which needs no API key.
# To use a hosted provider instead, change EMBEDDING_MODEL / CHAT_MODEL to a
# LiteLLM model name (for example "voyage/voyage-3" or "anthropic/claude-...")
# and set the matching API key as an environment variable.
EMBEDDING_MODEL=ollama/nomic-embed-text
EMBEDDING_DIMENSION=768
# On a machine with 16 GB, ollama/qwen2.5:7b measured much better (and makes /agent work) at
# about half the speed - see docs/evaluation.md. llama3.2 stays the default: it runs anywhere.
CHAT_MODEL=ollama/llama3.2
OLLAMA_BASE_URL=http://localhost:11434
# Sampling temperature of the answer model. 0 gives the same answer to the same question
# over the same context; without it a provider uses its own default (Ollama: 0.8), and a
# small local model can read one bullet under a different heading from run to run.
CHAT_TEMPERATURE=0

# Chunking
# Chunks are cut at line, sentence and word boundaries, never inside a word.
# A chunk shorter than CHUNK_MIN_SIZE (a name, a city, a lone URL) is merged into
# its neighbour; 0 turns that off. Only documents uploaded after a change are
# affected: re-upload a document to rebuild its chunks.
# A Markdown heading line (# ... ######) is never left as the last line of a chunk: it
# moves to the start of the next one, so it stays with the text it introduces.
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
CHUNK_MIN_SIZE=100

# Retrieval. "hybrid" merges vector and keyword search with reciprocal rank
# fusion; set to "vector" or "keyword" to use only one arm. CANDIDATES is how
# many rows each arm returns before fusion (kept larger than a request's
# top_k on purpose - see docs/plans/phase-2.md).
RETRIEVAL_MODE=hybrid
RETRIEVAL_CANDIDATES=20

# A word of a keyword query that is in more than this share of a tenant's chunks
# (a product name, "project") is left out of it: it says nothing about which chunk
# answers, and it lets every generic chunk take keyword credit. 1.0 turns it off.
# Not applied below 20 chunks, or to quoted phrases and -words.
KEYWORD_MAX_TERM_FREQUENCY=0.5

# When a query matches no chunk strictly, the keyword search retries it as an OR of
# its words. With this on, chunks are ranked by how RARE the words they contain are
# (a rare word outweighs several common ones) instead of PostgreSQL's ts_rank, which
# weighs every word the same. Only that fallback is affected, and only from 20 chunks.
KEYWORD_RARITY_WEIGHTING=true

# Reranking. Off by default - the default reranker needs no provider or API
# key. To enable it, set RERANK_ENABLED=true and choose a backend:
#   api  (default) a hosted rerank endpoint: set RERANK_MODEL to a LiteLLM rerank
#        model (for example "cohere/rerank-v3.5") and the matching API key.
#   chat  the chat model rates each of the best RERANK_CANDIDATES chunks against the
#        question (one model call each, 3-7 seconds for 10 on a 7B model on an M1 Pro).
#        Works with local Ollama, which has no rerank endpoint. RERANK_CHAT_MODEL
#        picks another model; empty reuses CHAT_MODEL. A 3B model barely helped, a 7B
#        model helped a lot - see docs/evaluation.md.
RERANK_ENABLED=false
RERANK_BACKEND=api
RERANK_MODEL=cohere/rerank-v3.5
RERANK_CHAT_MODEL=
RERANK_CANDIDATES=10

# What POST /query gives the answer model: each retrieved chunk plus this many
# chunks before and after it, joined into continuous excerpts (a chunk often
# ends with a heading whose bullets start the next one). 0 turns it off. Neighbours
# are only added while the context stays within the character limit below (the
# retrieved chunks are always kept): a local model has a small context window, and
# one that overflows loses the start of the prompt.
ANSWER_CONTEXT_NEIGHBOURS=1
ANSWER_CONTEXT_MAX_CHARS=6000

# Background jobs. Uploads larger than ASYNC_PROCESSING_THRESHOLD bytes are
# processed by the worker (docker compose up runs it as its own service)
# instead of within the upload request.
REDIS_URL=redis://localhost:6379/0
ASYNC_PROCESSING_THRESHOLD=100000

# Caching. Embeddings are cached by default (a hit can never be stale - it's
# keyed by model and text). Whole answers are cached only if enabled, keyed
# by a per-tenant corpus version that any upload or delete bumps.
EMBEDDING_CACHE_TTL=86400
ANSWER_CACHE_ENABLED=false
ANSWER_CACHE_TTL=3600

# Tracing and cost tracking. Off unless both keys are set - get them from
# a Langfuse project (cloud.langfuse.com or a self-hosted instance).
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# Agent mode (POST /agent). AGENT_MAX_STEPS is the hard ceiling on retrieval
# rounds per request. AGENT_PLANNER_MODEL is the LiteLLM model that decides
# what to search for next; empty reuses CHAT_MODEL. Planning needs reliable
# JSON output, so a stronger model here can help if CHAT_MODEL is small.
AGENT_MAX_STEPS=3
AGENT_PLANNER_MODEL=
